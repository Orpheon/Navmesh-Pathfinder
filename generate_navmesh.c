#include generate_navmesh.h

Navmesh* generate_navmesh(Bitmask *map)
{
    Navmesh *mesh = calloc(1, sizeof(Navmesh));

    for (y=0; y<map->height; y++)
    {
        for (x=0; x<map->width; x++)
        {
        }
    }
}
